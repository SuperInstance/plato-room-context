[package]
name = "plato-room-context"
version = "0.1.0"
edition = "2021"
description = "Context window manager — token budgets, eviction policies, priority stacking, sliding window"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
